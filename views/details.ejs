<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Place Finder</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
        <link href="style/detail-style.css" rel="stylesheet" />
</head>
<body>
    <div class="px-4 py-2 my-5 text-center">
        <h1 class="display-5 main-heading"><%= data.name %></h1>
        <div class="col-lg-6 mx-auto">
          <!-- <p class="lead mb-4">Quickly design and customize responsive mobile-first sites with Bootstrap, the world’s most popular front-end open source toolkit, featuring Sass variables and mixins, responsive grid system, extensive prebuilt components, and powerful JavaScript plugins.</p>
            -->
            <% if (data.rating){ %>
                <p>Rating: <%= data.rating%></p>
            <% } %>
            <% if (data. address){ %>
                <% if (data.address.street1 && data.address.street2 && data.address.city){ %>
                    <p>Address: <%= data.address.street1 %>,<%= data.address.street2 %>,<%= data.address.city %>, <%= data.address.country %></p>
                <% } else if (data.address.street1 && data.address.city) { %>
                    <p>Address: <%= data.address.street1 %>,<%= data.address.city %>, <%= data.address.country %></p>
                <% } else if (data.address.street2 && data.address.city) { %>
                        <p class="lead mb-4">Address: <%= data.address.street2 %>,<%= data.address.city %>, <%= data.address.country %></p>
                <% } else if(data.address.city) {%>
                    <p>Address: <%= data.address.city %>, <%= data.address.country %></p>
                <% } else{ %>
                    <p>Address: <%= data.address.country %></p>
                <% } %>
            <% } %>
            <% if (data.location){ %>
                <p>Latitude: <%= data.location.lat %>  Longitude: <%= data.location.long %></p>
            <% } %>
            <% if (data.phone){ %>
                <p>Phone: <%= data.phone%></p>
            <% } %>
            
            <div class="justify-data-sm-center">
                <% if (data.website){ %>
                    <button class="btn btn-outline-light btn-lg px-4"><a href="<%= data.website%>" target="_blank">Website</a></button>
                <% } %>
                <% if(data.photos_more){ %>
                    <button class="btn btn-dark btn-lg px-4 gap-3"><a href="<%= data.photos_more%>" target="_blank">See all photos</a></button>
                <% } %>
          </div>
        </div>
      </div>
    
    <div class="container images-div py-2 my-2">
        <div id="carouselExampleIndicators" class="carousel slide">
            <div class="carousel-indicators">
                <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
                <% for(let i=1; i<photos.length; i++) { %>
                    <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="<%= i %>" aria-label="Slide <%= i+1 %>"></button>
                <% } %>
            </div>
            <div class="carousel-inner">
                <% for(let i=0; i<photos.length; i++) { %>
                    <div class="carousel-item <%= i === 0 ? 'active' : '' %>">
                        <img src="<%= photos[i] %>" class="d-block images rounded" alt="image">
                    </div>
                <% } %>
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div>
    </div>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    </body>
</html>